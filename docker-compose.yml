x-nx-env: &nx-env
  NX_DAEMON: 'true'
  NX_DAEMON_SOCKET_DIR: /nx-sock
  NX_WATCHER: polling
  NX_WATCHER_POLLING_INTERVAL: '1000'
  NX_ISOLATE_PLUGINS: 'false'
  NX_PLUGIN_NO_TIMEOUTS: 'true'
  PACKMIND_EDITION: ${PACKMIND_EDITION:-proprietary}

services:
  nx-daemon:
    profiles:
      - dev
    image: node:24.15.0-alpine3.23
    container_name: ${COMPOSE_PROJECT_NAME:-dev}-${PACKMIND_EDITION:-proprietary}-packmind-nx-daemon
    working_dir: /packmind
    command: >
      sh -lc "
        set -eu;
        mkdir -p /nx-sock && chmod 777 /nx-sock;
        ./node_modules/.bin/nx daemon --start;
        for i in $$(seq 1 30); do
          [ -S /nx-sock/d.sock ] && break;
          [ $$i -eq 30 ] && exit 1;
          sleep 1;
        done;
        echo '[nx-daemon] Socket ready, pre-warming project graph...';
        ./node_modules/.bin/nx show projects >/dev/null &
        tail -f /dev/null
      "
    volumes:
      - .:/packmind
      - dev-node_modules:/packmind/node_modules
      - dev-nx-sock:/nx-sock
    environment:
      <<: *nx-env
    healthcheck:
      test: ['CMD-SHELL', 'test -S /nx-sock/d.sock']
      interval: 1s
      timeout: 1s
      retries: 30
      start_period: 3s
    depends_on:
      install-dependencies:
        condition: service_completed_successfully

  postgres:
    image: postgres:17-alpine
    container_name: ${COMPOSE_PROJECT_NAME:-dev}-${PACKMIND_EDITION:-proprietary}-packmind-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: packmind
    ports:
      - '5433:5432'
    volumes:
      - dev-postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres']
      interval: 1s
      timeout: 2s
      retries: 10
      start_period: 2s

  redis:
    image: redis:7.2.4
    container_name: ${COMPOSE_PROJECT_NAME:-dev}-${PACKMIND_EDITION:-proprietary}-redis
    ports:
      - '6380:6379'
    volumes:
      - dev-redis-data:/data
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 1s
      timeout: 1s
      retries: 10
      start_period: 1s

  pgadmin:
    profiles:
      - dev
    image: dpage/pgadmin4:latest
    container_name: ${COMPOSE_PROJECT_NAME:-dev}-${PACKMIND_EDITION:-proprietary}-packmind-pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@pgadmin.com
      PGADMIN_DEFAULT_PASSWORD: password
      PGADMIN_LISTEN_PORT: 80
    ports:
      - '2346:80'
    volumes:
      - dev-pgadmin:/var/lib/pgadmin
    depends_on:
      - postgres

  install-dependencies:
    image: node:24.15.0-alpine3.23
    container_name: ${COMPOSE_PROJECT_NAME:-dev}-${PACKMIND_EDITION:-proprietary}-packmind-install-deps
    working_dir: /packmind
    environment:
      HUSKY: '0'
    entrypoint:
      - sh
      - -c
      - |
        apk add --no-cache git
        corepack enable
        corepack prepare pnpm@11.5.0 --activate
        if ! pnpm install --frozen-lockfile --prefer-offline; then
          echo "[deps] lockfile drift detected — run 'pnpm install' locally and commit pnpm-lock.yaml" >&2
          exit 1
        fi
        chmod -R +x node_modules/.bin 2>/dev/null || true
        node scripts/select-tsconfig.mjs
    volumes:
      - .:/packmind
      - dev-node_modules:/packmind/node_modules
      - dev-corepack-cache:/root/.cache/node/corepack
      - dev-pnpm-store:/root/.local/share/pnpm

  run-migrations:
    image: node:24.15.0-alpine3.23
    container_name: ${COMPOSE_PROJECT_NAME:-dev}-${PACKMIND_EDITION:-proprietary}-packmind-run-migrations
    working_dir: /packmind
    entrypoint: >
      sh -lc '
        set -euo pipefail
        echo "[migrations] Running TypeORM migrations directly (bypassing Nx)..."
        corepack enable
        corepack prepare pnpm@11.5.0 --activate
        cd packages/migrations
        pnpm typeorm migration:run --dataSource=datasourceDocker.ts --transaction each
      '
    environment:
      CI: ${CI:-}
      DATABASE_URL: postgres://postgres:postgres@postgres:5432/packmind
      PACKMIND_EDITION: ${PACKMIND_EDITION:-proprietary}
    volumes:
      - .:/packmind
      - dev-node_modules:/packmind/node_modules
      - dev-corepack-cache:/root/.cache/node/corepack
      - dev-pnpm-store:/root/.local/share/pnpm
    depends_on:
      install-dependencies:
        condition: service_completed_successfully
      postgres:
        condition: service_healthy

  frontend:
    image: node:24.15.0-alpine3.23
    container_name: ${COMPOSE_PROJECT_NAME:-dev}-${PACKMIND_EDITION:-proprietary}-packmind-frontend
    working_dir: /packmind
    ports:
      - '4201:4200'
    entrypoint: >
      sh -lc '
        set -eu
        if [ -n "${CI:-}" ]; then
          echo "[frontend] CI mode -> WITHOUT daemon"
          export NX_DAEMON=false; exec ./node_modules/.bin/nx run frontend:dev
        fi
        if [ -S /nx-sock/d.sock ]; then
          echo "[frontend] Daemon OK -> WITH daemon"
          exec ./node_modules/.bin/nx run frontend:dev
        else
          echo "[frontend] Daemon NOT reachable -> WITHOUT daemon"
          export NX_DAEMON=false; exec ./node_modules/.bin/nx run frontend:dev
        fi
      '
    environment:
      <<: *nx-env
      CI: ${CI:-}
      API_HOSTNAME: 'backend'
      API_PORT: 3000
      MCP_HOSTNAME: 'mcp-server'
      MCP_PORT: 3001
      CHOKIDAR_USEPOLLING: '1'
      CHOKIDAR_INTERVAL: '1000'

    volumes:
      - .:/packmind
      - dev-node_modules:/packmind/node_modules
      - dev-dist:/packmind/dist
      - dev-tmp:/packmind/tmp
      - dev-nx-sock:/nx-sock
    healthcheck:
      test: wget --spider http://127.0.0.1:4200/
    depends_on:
      nx-daemon:
        condition: service_healthy
        required: false
      install-dependencies:
        condition: service_completed_successfully

  backend:
    image: node:24.15.0-alpine3.23
    container_name: ${COMPOSE_PROJECT_NAME:-dev}-${PACKMIND_EDITION:-proprietary}-packmind-backend
    working_dir: /packmind
    entrypoint: >
      sh -lc '
        set -eu
        apk update && \
        apk add --no-cache \
                bash \
                ca-certificates && \
        if [ ! -d "$$JS_PLAYGROUND_PATH" ]; then \
          cp -R packages/linter/js-playground $$JS_PLAYGROUND_PATH || true; \
        fi && \
        if [ -n "${CI:-}" ]; then
          echo "[backend] CI mode -> WITHOUT daemon"
          export NX_DAEMON=false; exec ./node_modules/.bin/nx run api:serve:development --skip-nx-cache
        fi
        if [ -S /nx-sock/d.sock ]; then
          echo "[backend] Daemon OK -> WITH daemon"
          exec ./node_modules/.bin/nx run api:serve:development --skip-nx-cache
        else
          echo "[backend] Daemon NOT reachable -> WITHOUT daemon"
          export NX_DAEMON=false; exec ./node_modules/.bin/nx run api:serve:development --skip-nx-cache
        fi
      '
    environment:
      <<: *nx-env
      CI: ${CI:-}
      DATABASE_URL: postgres://postgres:postgres@postgres:5432/packmind
      REDIS_URI: redis://redis:6379
      COOKIE_SECURE: 'false'
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      CHOKIDAR_USEPOLLING: '1'
      CHOKIDAR_INTERVAL: '200'
      WATCHPACK_POLLING: 'true'
      JS_PLAYGROUND_PATH: 'packages/linter/js-playground-local'
      APP_WEB_URL: ${APP_WEB_URL:-http://localhost:4200}

    volumes:
      - .:/packmind
      - dev-node_modules:/packmind/node_modules
      - dev-dist:/packmind/dist
      - dev-tmp:/packmind/tmp
      - dev-nx-sock:/nx-sock
    healthcheck:
      test: wget --spider http://127.0.0.1:3000/api/v0
    depends_on:
      nx-daemon:
        condition: service_healthy
        required: false
      run-migrations:
        condition: service_completed_successfully
      redis:
        condition: service_healthy

  mcp-server:
    image: node:24.15.0-alpine3.23
    container_name: ${COMPOSE_PROJECT_NAME:-dev}-${PACKMIND_EDITION:-proprietary}-packmind-mcp-server
    working_dir: /packmind
    entrypoint: >
      sh -lc '
        set -eu
        if [ ! -d "$$JS_PLAYGROUND_PATH" ]; then \
          cp -R packages/linter/js-playground $$JS_PLAYGROUND_PATH || true; \
        fi && \
        if [ -n "${CI:-}" ]; then
          echo "[mcp] CI mode -> WITHOUT daemon"
          export NX_DAEMON=false; exec ./node_modules/.bin/nx run mcp-server:serve:development --host=0.0.0.0 --verbose
        fi
        if [ -S /nx-sock/d.sock ]; then
          echo "[mcp] Daemon OK -> WITH daemon"
          exec ./node_modules/.bin/nx run mcp-server:serve:development --host=0.0.0.0 --verbose
        else
          echo "[mcp] Daemon NOT reachable -> WITHOUT daemon"
          export NX_DAEMON=false; exec ./node_modules/.bin/nx run mcp-server:serve:development --host=0.0.0.0 --verbose
        fi
      '
    environment:
      <<: *nx-env
      CI: ${CI:-}
      DATABASE_URL: postgres://postgres:postgres@postgres:5432/packmind
      REDIS_URI: redis://redis:6379
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      JS_PLAYGROUND_PATH: 'packages/linter/js-playground-local'
    volumes:
      - .:/packmind
      - dev-node_modules:/packmind/node_modules
      - dev-dist:/packmind/dist
      - dev-tmp:/packmind/tmp
      - dev-nx-sock:/nx-sock
    depends_on:
      nx-daemon:
        condition: service_healthy
        required: false
      run-migrations:
        condition: service_completed_successfully

  nginx:
    image: nginx:alpine
    container_name: ${COMPOSE_PROJECT_NAME:-dev}-${PACKMIND_EDITION:-proprietary}-packmind-nginx
    ports:
      - '443:443'
    entrypoint:
      - sh
      - -c
      - |
        apk add --no-cache openssl &&
        mkdir -p /etc/nginx/ssl &&
        openssl req -x509 -nodes -days 365 -newkey rsa:2048 \
          -keyout /etc/nginx/ssl/key.pem \
          -out /etc/nginx/ssl/cert.pem \
          -subj "/C=US/ST=Dev/L=Local/O=Packmind/CN=localhost" &&
        nginx -g "daemon off;"
    volumes:
      - ./docker/nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./docker/nginx/ssl/:/etc/nginx/ssl
    depends_on:
      frontend:
        condition: service_healthy

  run-e2e-tests:
    image: mcr.microsoft.com/playwright:v1.58.2-jammy
    container_name: ${COMPOSE_PROJECT_NAME:-dev}-run-e2e-tests-${PACKMIND_EDITION:-proprietary}
    profiles:
      - e2e
    volumes:
      - .:/packmind
      - dev-node_modules:/packmind/node_modules
      - dev-dist:/packmind/dist
      - dev-tmp:/packmind/tmp
      - dev-nx-sock:/nx-sock
      - ./dist/.playwright:/packmind/dist/.playwright
      - dev-corepack-cache:/root/.cache/node/corepack
      - dev-pnpm-store:/root/.local/share/pnpm
    ports:
      - '9323:9323'
    working_dir: /packmind/apps/e2e-tests
    entrypoint: sh -c "corepack enable && corepack prepare pnpm@11.5.0 --activate && cd /packmind && pnpm e2e"
    environment:
      BASE_URL: http://frontend:4200
      CI: 'true'
      NODE_OPTIONS: '--max-old-space-size=16384'
    depends_on:
      frontend:
        condition: service_healthy
      backend:
        condition: service_healthy

volumes:
  dev-nx-sock:
  dev-postgres-data:
  dev-redis-data:
  dev-pgadmin:
  dev-dist:
  dev-tmp:
  dev-node_modules:
  dev-corepack-cache:
  dev-pnpm-store:
